pub enum OnlinePackets {
     ConnectedPing,
     ConnectedPong,
     ConnectionRequest,
     ConnectionAccept,
     NewConnection,
     Disconnect,
     Incompatible
}

impl OnlinePackets {
     // pub fn recv(byte: u8) -> Self {

     // }
}